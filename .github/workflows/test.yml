name: Test

on:
  push:
    paths-ignore:
      - "docs/**"
      - "README.md"
      - "LICENSE"
      - ".editorconfig"
    branches: [main, master]
  pull_request:
    paths-ignore:
      - "docs/**"
      - "README.md"
      - "LICENSE"
      - ".editorconfig"
    branches: [main, master]

permissions:
  contents: read

jobs:
  test:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
        node: [22.x, 24.x, 25.x]

    steps:
      - name: ⬇️ Checkout repository
        uses: actions/checkout@v4

      - name: ⚙️ Enable Corepack
        run: corepack enable

      - name: ⚙️ Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node }}
          cache: "yarn"

      - name: 📦 Install dependencies
        shell: bash
        run: yarn install --immutable

      - name: 🎭 Install Playwright Browsers
        # This installs the browser binaries needed by Vitest
        run: npx playwright install chromium --with-deps

      - name: 🏗️ Build library
        shell: bash
        run: yarn ng build ngx-toast

      - name: 🧪 Run tests
        shell: bash
        # For Vitest-based Angular testing in CI
        run: yarn ng test ngx-toast --watch=false --browsers.name=chromium --browsers.headless=true

      - name: 🛡️ Security Audit (Linux only)
        if: matrix.os == 'ubuntu-latest' && success()
        shell: bash
        run: |
          if [ -f "./shai-hulud-audit.sh" ]; then
            chmod +x ./shai-hulud-audit.sh
            ./shai-hulud-audit.sh --no-color --save-report || true
          else
            echo "Audit script not found, skipping."
          fi
        continue-on-error: true

      - name: 📤 Upload security audit report
        if: matrix.os == 'ubuntu-latest'
        uses: actions/upload-artifact@v4
        with:
          name: security-audit-node${{ matrix.node }}-${{ github.run_number }}
          path: "*.log"
          retention-days: 7